{
  "name": "@comet/edge-auth",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "build:tsc": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.888.0",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^22.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  }
}
