{
  "name": "@comet/edge-auth",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "build:tsc": "tsc",
    "test": "vitest run src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1119.0",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.3.0",
    "vitest": "^4.1.11"
  }
}
