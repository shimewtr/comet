{
  "name": "@comet/stamp-upload",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "test": "vitest run src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@comet/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.110",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.3.0",
    "vitest": "^4.1.11"
  }
}
